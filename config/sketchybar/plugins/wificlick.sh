#!/bin/bash
# Native Wi-Fi settings pane. (Upstream launched btop in kitty here, which
# both contradicted network.sh's own click handler and needs tools we
# don't have.)
open "x-apple.systempreferences:com.apple.wifi-settings-extension"
